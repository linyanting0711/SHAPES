interface Drawable {
    // defining the 3 methods required
    void draw();
    // defining the return type as double which can be changed if required to the correct datatype
    double getArea();
    double getPerimeter();
}